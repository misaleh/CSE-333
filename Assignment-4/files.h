#ifndef FILES_H
#define FILES_H
#include "essentials.h"

void readFile(char *fileName ,int *number_processes , process p[]); /*read from file called file name , return number of processes and array of structs with each parameter for each process , each process has a struct*/
#endif
