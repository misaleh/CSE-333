#ifndef QUEUE_H
#define QUEUE_H
#include "essentials.h"
void enqueue(int *first , int *last ,process *element, process *arr[]); /*add element to certian queue*/  
int dequeue(int *first , int *last , process *arr[]); /*remove element form certain queue*/ 
#endif
